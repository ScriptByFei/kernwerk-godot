name: Test, Build & Deploy (GitHub Pages)

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GODOT_VERSION: 4.6.3
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install Godot
        shell: bash
        run: |
          curl --fail --location --silent --show-error \
            --output godot.zip \
            "https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}-stable/Godot_v${GODOT_VERSION}-stable_linux.x86_64.zip"
          unzip -q godot.zip
          chmod +x "Godot_v${GODOT_VERSION}-stable_linux.x86_64"

      - name: Import project and register global classes
        shell: bash
        run: |
          set -euo pipefail
          godot_bin="$PWD/Godot_v${GODOT_VERSION}-stable_linux.x86_64"
          exit_code=0
          output="$(timeout 60s "$godot_bin" --headless --editor --path . --quit 2>&1)" || exit_code=$?
          echo "$output"
          if [ "$exit_code" -ne 0 ] || grep -Eq 'SCRIPT ERROR|^ERROR:' <<<"$output"; then
            echo "Godot project import failed (exit=$exit_code)" >&2
            exit 1
          fi

      - name: Run headless suites
        shell: bash
        run: |
          set -euo pipefail
          godot_bin="$PWD/Godot_v${GODOT_VERSION}-stable_linux.x86_64"
          for suite in tests/*_test.gd; do
            echo "===== $suite ====="
            exit_code=0
            output="$(timeout 60s "$godot_bin" --headless --path . -s "$suite" 2>&1)" || exit_code=$?
            echo "$output"
            if [ "$exit_code" -ne 0 ] || grep -Eq 'SCRIPT ERROR|^ERROR:' <<<"$output"; then
              echo "Godot test failed in $suite (exit=$exit_code)" >&2
              exit 1
            fi
          done

      - name: Smoke-test main scene
        shell: bash
        run: |
          set -euo pipefail
          godot_bin="$PWD/Godot_v${GODOT_VERSION}-stable_linux.x86_64"
          exit_code=0
          output="$(timeout 30s "$godot_bin" --headless --path . --quit-after 120 2>&1)" || exit_code=$?
          echo "$output"
          if [ "$exit_code" -ne 0 ] || grep -Eq 'SCRIPT ERROR|^ERROR:' <<<"$output"; then
            echo "Godot main-scene smoke failed (exit=$exit_code)" >&2
            exit 1
          fi

  build:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          lfs: false

      - name: Godot headless export (Web)
        id: godot
        uses: firebelley/godot-export@v8.0.0
        with:
          godot_executable_download_url: https://github.com/godotengine/godot/releases/download/4.6.3-stable/Godot_v4.6.3-stable_linux.x86_64.zip
          godot_export_templates_download_url: https://github.com/godotengine/godot/releases/download/4.6.3-stable/Godot_v4.6.3-stable_export_templates.tpz
          relative_project_path: ./
          archive_output: false

      - name: Publish gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ${{ steps.godot.outputs.build_directory }}/Web
          publish_branch: gh-pages
          force_orphan: true
